The file wideband_image.h is generated for use by gerEFI to be able to beam firmware updates to attached wideband controllers.

Use ``cd firmware/boards/f0_module ; ./build_wideband.sh`` to build a fresh version.